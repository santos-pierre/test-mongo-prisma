import ContainerWithPadding from '../ui/ContainerWithPadding';

const AppLayout: React.FC = ({ children }) => {
    return <ContainerWithPadding>{children}</ContainerWithPadding>;
};

export default AppLayout;
